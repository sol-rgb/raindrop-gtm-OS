{
  "name": "raindrop-gtm-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Raindrop GTM OS. Outbound in one place: pipeline, the signals feeding it, each channel, and the replies waiting on us.",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "migrate": "node scripts/migrate.js",
    "probe": "node scripts/probe.js",
    "sync": "node worker/run.js",
    "selftest": "node scripts/derive-selftest.js"
  },
  "dependencies": {
    "next": "16.3.5",
    "pg": "^8.12.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "8.5.28",
    "tailwindcss": "^3.4.7"
  }
}
